------------------------------------------------------------------- JSX -------------------------------------------------------------------


const experiences = [
  {
    title: "Casa do Cinema Português",
    description:
      "Worked on design projects including posters, magazines, and promotional materials for film screenings.",
    photos: [
      "/photos/IMG_20250504_174100.jpg",
      "/photos/IMG_20240815_124924.jpg",
      "/photos/IMG_20250601_210335.jpg",
      "/photos/IMG_20250516_221026.jpg",
      "/photos/IMG_20250601_210335.jpg",
    ],
  },
  {
    title: "Associação Caminhos do Cinema Português",
    description:
      "Developed agendas, visual identities, and communication materials for events.",
    photos: [
      "/photos/IMG_20240815_124924.jpg",
      "/photos/IMG_20250504_174100.jpg",
      "/photos/IMG_20250601_210335.jpg",
    ],
  },
];

<section className="experience-section">
    <h3>Experiences</h3>

    <div className="experience-list">
        {experiences.map((exp, idx) => (
        <article key={idx} className="experience-card">
            {/* left: collage */}
            <div className="collage" aria-hidden="true">
            {exp.photos.map((photo, pIdx) => {
                const posClasses = ['p1','p2','p3','p4','p5','p6'];
                const cls = posClasses[pIdx % posClasses.length];
                return (
                <div key={pIdx} className={`collage-item ${cls}`}>
                    <img src={photo} alt={`${exp.title} photo ${pIdx+1}`} />
                </div>
                );
            })}
            </div>

            {/* right: text */}
            <div className="experience-text">
            <h4 className="exp-title">{exp.title}</h4>
            <p className="exp-desc">{exp.description}</p>
            </div>
        </article>
        ))}
    </div>
</section>




------------------------------------------------------------------- CSS -------------------------------------------------------------------

/* Experience section wrapper */
.experience-section {
  margin: 4vw 0;
  padding: 2rem 2rem;
  box-sizing: border-box;
}
.experience-section h3 {
  color: #6d2c5b;
  font-size: clamp(1.25rem, 2.6vw, 1.6rem);
  margin-bottom: 1.25rem;
}
/* Each experience as a card */
.experience-list {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
.experience-card {
  display: flex;
  gap: 1.25rem;
  align-items: stretch;
  background: white;
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.07);
  padding: 1rem;
  overflow: visible; /* allow images to overlap edges slightly */
}
/* Collage column */
.collage {
  width: 44%;
  min-width: 260px;
  max-width: 420px;
  position: relative;
  aspect-ratio: 4 / 3; /* keeps the collage area proportioned */
  background: transparent;
  flex-shrink: 0;
}
/* one image holder (absolutely positioned) */
.collage-item {
  position: absolute;
  width: 46%;              /* default image size; some presets will override */
  height: auto;
  transition: transform 220ms ease, box-shadow 220ms ease;
  transform-origin: center center;
  z-index: 1;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 6px 20px rgba(0,0,0,0.12);
  background: #eee;
}
/* the actual image fills its container */
.collage-item img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
  transform: translateZ(0);
}
/* Position presets - tweak numbers to taste.
   The goal is layered, slightly-rotated, partially overlapping items. */
.collage-item.p1 { left: 6%; top: 6%; width: 54%; transform: rotate(-6deg) scale(1); z-index: 3; }
.collage-item.p2 { right: 8%; top: 2%; width: 48%; transform: rotate(6deg) scale(0.98); z-index: 4; }
.collage-item.p3 { left: 10%; bottom: 8%; width: 44%; transform: rotate(4deg) scale(0.96); z-index: 2; }
.collage-item.p4 { right: 18%; bottom: 12%; width: 36%; transform: rotate(-8deg) scale(0.94); z-index: 1; }
.collage-item.p5 { left: 28%; top: 28%; width: 40%; transform: rotate(10deg) scale(0.92); z-index: 5; opacity: 0.98; }
.collage-item.p6 { left: 48%; top: 12%; width: 36%; transform: rotate(-4deg) scale(0.95); z-index: 6; }

/* Hover: bring the hovered photo on top and pop slightly */
.collage-item:hover {
  transform: scale(1.03) translateY(-6px) rotate(0deg);
  z-index: 10 !important;
  box-shadow: 0 18px 40px rgba(0,0,0,0.18);
}

/* Text column */
.experience-text {
  flex: 1;
  min-width: 240px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.exp-title {
  margin: 0;
  font-size: clamp(1rem, 1.8vw, 1.2rem);
  color: #2f2f2f;
  font-weight: 700;
}
.exp-desc {
  margin: 0.5rem 0 0 0;
  color: #444;
  line-height: 1.5;
  font-size: clamp(0.95rem, 1.4vw, 1rem);
}

/* responsive: stack on narrow screens */
@media (max-width: 880px) {
  .experience-card {
    flex-direction: column;
    gap: 0.75rem;
  }
  .collage {
    width: 100%;
    min-width: 0;
    aspect-ratio: auto;
    height: 220px;   /* fixed height for mobile collage */
  }
  /* turn collage into neat grid on mobile to avoid overlap issues */
  .collage-item {
    position: relative;
    width: calc(33.333% - 6px);
    height: calc(50% - 6px);
    margin: 3px;
    float: left;
    transform: none !important;
    z-index: auto !important;
    border-radius: 8px;
    box-shadow: 0 6px 18px rgba(0,0,0,0.08);
  }
  .collage-item img { height: 100%; object-fit: cover; }
}

/* tweak spacing for very small screens */
@media (max-width: 520px) {
  .experience-card { padding: 0.75rem; }
  .collage { height: 180px; }
  .exp-title { font-size: 1rem; }
}